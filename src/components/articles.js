import React from "react";
// Styled
import { Articles as ArticlesStyled } from "../styles/articles";

export const Articles = ({ children }) => {
  return <ArticlesStyled>{children}</ArticlesStyled>;
};
